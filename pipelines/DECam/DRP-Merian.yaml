description: >
  The DRP pipeline specialized for the DECam instrument, developed against the
  Merian dataset.
imports:
  - $DRP_PIPE_DIR/ingredients/DECam/DRP-Base.yaml
  - $DRP_PIPE_DIR/ingredients/DECam/DRP-GatherResourceStatistics.yaml

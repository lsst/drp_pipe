# Recommended configs for compression for LSSTCam DRP, when using butler
# repositories with _future suffixes.
#
# Use it by adding
#
#  includeConfigs:
#    - ${DRP_PIPE_DIR}/bps/compression/LSSTCam/DRP-compression_future.yaml
#
# (with no outer indentation) to your BPS config file.
#
# Including this file is incompatible with including any other caching or
# compression config yamls that set DAF_BUTLER_CONFIG_PATH (TODO DM-53875)

environment:
  DAF_BUTLER_CONFIG_PATH: ${DRP_PIPE_DIR}/bps/compression/LSSTCam/DRP-config_future:${DAF_BUTLER_CONFIG_PATH}
